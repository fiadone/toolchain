// const wordpress = require('../../middlewares/wordpress-proxy')

module.exports = function (router) {
  // Here you can:
  //   a) set static routes: router.get('/', require('./homepage'))
  //   b) use wordpress middleware: router.get('*', wordpress)
  //   c) use your own middleware: router.get('*', yourMiddleware)
}
